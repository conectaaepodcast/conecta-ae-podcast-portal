# Público (exposto ao browser) — URL do projeto Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Apenas servidor (NUNCA prefixo NEXT_PUBLIC_) — uso mínimo em rotas protegidas
# SUPABASE_SERVICE_ROLE_KEY=

# URL canônica do site (SEO / Open Graph)
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
